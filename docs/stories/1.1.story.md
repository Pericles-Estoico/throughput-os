# Story 1.1 — Sales Intelligence: Importação de Vendas e Cálculo de Ganho

## Status
Ready for Dev

## Story
**Como** usuário do Throughput OS,
**Quero** importar dados de vendas por SKU e ver o ganho (throughput) calculado automaticamente,
**Para** saber quais produtos realmente geram dinheiro e tomar decisões baseadas em ganho real.

## Acceptance Criteria

1. **Tabela de vendas** exibe: SKU, Canal, Quantidade, Preço Médio, Receita Líquida, Ganho Unitário, Ganho Total
2. **Formulário de importação** permite inserir venda manual com todos os campos da tabela `sales`
3. **Ganho unitário** calculado automaticamente: `net_revenue - variable_cost_total`
4. **Ganho total** calculado: `throughput_unit × quantity`
5. **Ranking automático** por ganho total (maior → menor)
6. **Filtros** funcionais: período (data), canal, categoria
7. **Estados vazios** exibidos quando não há dados
8. **Validação** de campos obrigatórios no formulário

## Tasks

- [ ] 1. Criar Server Action para buscar vendas com métricas calculadas
  - [ ] 1.1 Query que faz JOIN de sales + costs + skus
  - [ ] 1.2 Calcular throughput_unit e throughput_total no servidor
  - [ ] 1.3 Ordenar por throughput_total desc por padrão

- [ ] 2. Criar componente SalesTable com TanStack Table
  - [ ] 2.1 Colunas: sku_code, product_name, channel, quantity, unit_price, net_revenue, throughput_unit, throughput_total
  - [ ] 2.2 Sorting por coluna
  - [ ] 2.3 Formatação monetária (R$) nas colunas numéricas

- [ ] 3. Criar formulário de importação manual de venda
  - [ ] 3.1 Campos: sku_id (select), date, channel, quantity, unit_price, gross_revenue, fees, freight, tax
  - [ ] 3.2 Validação com Zod
  - [ ] 3.3 Server Action para inserir na tabela sales
  - [ ] 3.4 Feedback de sucesso/erro para o usuário

- [ ] 4. Implementar filtros na página /dashboard/sales
  - [ ] 4.1 Filtro de período (date range)
  - [ ] 4.2 Filtro de canal (select)
  - [ ] 4.3 Filtro de categoria (select)
  - [ ] 4.4 Aplicar filtros na query server-side

- [ ] 5. Atualizar página /dashboard/sales com todos os componentes
  - [ ] 5.1 Layout: header + filtros + tabela + botão importar
  - [ ] 5.2 Estado vazio com instrução clara
  - [ ] 5.3 Loading skeleton enquanto carrega

## Dev Notes

**Stack relevante:**
- Next.js App Router com Server Actions (sem API routes)
- TanStack Table para a tabela de dados
- React Hook Form + Zod para o formulário
- Supabase client server-side via `src/lib/supabase/server.ts`
- Funções de cálculo em `src/lib/calculations.ts` (já implementadas)
- Tipos em `src/types/index.ts` (já definidos)

**Schema Supabase (tabelas envolvidas):**
- `sales`: id, sku_id, date, channel, quantity, unit_price, gross_revenue, fees, freight, tax, net_revenue (computed)
- `costs`: sku_id, variable_cost_total (computed)
- `skus`: id, sku_code, product_name, category, family, active

**Query base para métricas:**
```sql
SELECT
  s.id, s.date, s.channel, s.quantity, s.unit_price,
  s.net_revenue,
  sk.sku_code, sk.product_name, sk.category,
  c.variable_cost_total,
  (s.net_revenue - c.variable_cost_total) as throughput_unit,
  (s.net_revenue - c.variable_cost_total) * s.quantity as throughput_total
FROM sales s
JOIN skus sk ON sk.id = s.sku_id
LEFT JOIN costs c ON c.sku_id = s.sku_id
ORDER BY throughput_total DESC
```

**Estrutura de arquivos esperada:**
```
src/app/dashboard/sales/
  page.tsx                    ← atualizar (scaffold existe)
  actions.ts                  ← Server Actions (criar)
src/components/sales/
  sales-table.tsx             ← TanStack Table (criar)
  sale-form.tsx               ← Formulário de importação (criar)
  sales-filters.tsx           ← Filtros (criar)
```

## File List
<!-- Preencher pelo @dev durante implementação -->

## Dev Agent Record

### Agent Model Used
<!-- Preencher pelo @dev -->

### Debug Log References
<!-- Preencher pelo @dev se necessário -->

### Completion Notes
<!-- Preencher pelo @dev ao concluir -->

### Change Log
<!-- Preencher pelo @dev -->
