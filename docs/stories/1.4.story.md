# Story 1.4 — Inventory Control: Estoque e Cobertura

## Status
Ready for Review

## Story
**Como** usuário do Throughput OS,
**Quero** ver o estoque de cada SKU com cobertura em dias e alertas automáticos,
**Para** saber onde o dinheiro está preso e agir antes da ruptura.

## Acceptance Criteria

1. **Tabela** exibe: SKU, Estoque Total, Venda Média Diária, Cobertura (dias), Status
2. **Status calculado automaticamente**: ruptura (<3d), saudável (3-15d), excesso (>15d)
3. **Badge visual** por status: ruptura=vermelho, saudável=verde, excesso=amarelo
4. **Alertas automáticos** destacados no topo: produtos em ruptura + produtos com excesso
5. **Estoque total** = stock_raw + stock_semi + stock_finished + stock_marketplace
6. **Venda média diária** calculada dos últimos 30 dias de vendas
7. **Ordenação** por cobertura crescente (ruptura primeiro)

## Tasks

- [x] 1. Criar Server Action para buscar dados de estoque
  - [x] 1.1 JOIN inventory + skus
  - [x] 1.2 Calcular venda média diária (últimos 30d) da tabela sales
  - [x] 1.3 Calcular stock_total = raw + semi + finished + marketplace
  - [x] 1.4 Aplicar `calculateStockCoverage()` e `getStockStatus()` de calculations.ts
  - [x] 1.5 Ordenar por coverage_days asc

- [x] 2. Criar componente InventoryTable
  - [x] 2.1 Colunas: sku_code, product_name, stock_total, avg_daily_sales, coverage_days, status
  - [x] 2.2 Badge por status com cores corretas
  - [x] 2.3 Highlight de linha para ruptura

- [x] 3. Criar painel de alertas de estoque
  - [x] 3.1 Seção "Atenção imediata": SKUs com ruptura (<3d)
  - [x] 3.2 Seção "Excesso": SKUs com cobertura >15d
  - [x] 3.3 Contadores: X em ruptura, Y em excesso

- [x] 4. Atualizar página /dashboard/inventory
  - [x] 4.1 Header com alertas no topo
  - [x] 4.2 InventoryTable abaixo
  - [x] 4.3 Estado vazio com instrução

## Dev Notes

**Funções em `src/lib/calculations.ts`:**
- `calculateStockCoverage(stock, avgDailySales)`
- `getStockStatus(coverageDays)` → 'ruptura' | 'saudavel' | 'excesso'

**Tipos:**
- `StockStatus = 'ruptura' | 'saudavel' | 'excesso'`
- `Inventory` interface em types/index.ts

**Estrutura de arquivos esperada:**
```
src/app/dashboard/inventory/
  page.tsx                    ← atualizar
  actions.ts                  ← Server Actions (criar)
src/components/inventory/
  inventory-table.tsx         ← criar
  stock-alerts.tsx            ← criar
  stock-status-badge.tsx      ← criar
```

## File List

- `src/app/dashboard/inventory/actions.ts` — criado — getInventoryData() com 3 queries paralelas
- `src/app/dashboard/inventory/page.tsx` — modificado — async server component com contadores
- `src/components/inventory/stock-status-badge.tsx` — criado — badge com cores por status
- `src/components/inventory/inventory-table.tsx` — criado — tabela com highlight de ruptura
- `src/components/inventory/stock-alerts.tsx` — criado — painéis ruptura + excesso

## Dev Agent Record

### Agent Model Used
claude-sonnet-4-6 (@dev / Dex)

### Debug Log References
— sem issues

### Completion Notes
- `getInventoryData()` usa Promise.all com 3 queries (inventory, sales 30d, skus)
- `calculateStockCoverage(stock, 0)` retorna 999 — formatado como "—" na tabela
- SKUs com coverage >= 999 classificados como 'excesso' (sem venda = capital preso)
- `StockAlerts` cap de 5 items no painel de excesso para não sobrecarregar a UI
- Lint: 0 erros (3 warnings pré-existentes)
- TypeScript: sem erros
- Build: `✓ Compiled successfully` — `/dashboard/inventory` como `ƒ Dynamic`

### Change Log
- Criado `src/app/dashboard/inventory/actions.ts`
- Atualizado `src/app/dashboard/inventory/page.tsx`
- Criado `src/components/inventory/stock-status-badge.tsx`
- Criado `src/components/inventory/inventory-table.tsx`
- Criado `src/components/inventory/stock-alerts.tsx`
