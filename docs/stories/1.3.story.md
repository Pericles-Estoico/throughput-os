# Story 1.3 — Execution Dashboard: O Que Fazer Hoje

## Status
Ready for Review

## Story
**Como** usuário do Throughput OS,
**Quero** abrir o sistema e ver imediatamente o que devo fazer hoje,
**Para** tomar decisões sem precisar analisar dados manualmente.

## Acceptance Criteria

1. **4 cards de KPIs** exibem dados reais: Ganho do Dia, SKUs A vendidos hoje, SKUs em ruptura, Gargalo atual
2. **Ação crítica do dia** gerada automaticamente com base nos alertas ativos
3. **5 tipos de alertas** detectados automaticamente:
   - Produto A em ruptura (cobertura < 3 dias)
   - Produto D consumindo produção
   - Gargalo parado (sem vendas do dia)
   - Estoque acima de 20 dias
   - Venda sem margem (throughput negativo)
4. **Lista de alertas** exibida com prioridade (crítico primeiro)
5. **Dashboard atualiza** os dados a cada acesso (sem cache estático)

## Tasks

- [x] 1. Criar Server Actions para dados do dashboard
  - [x] 1.1 `getDailyThroughput()` — soma throughput do dia atual
  - [x] 1.2 `getSkusASoldToday()` — contagem SKUs classe A com venda hoje
  - [x] 1.3 `getSkusInRupture()` — SKUs com cobertura < 3 dias
  - [x] 1.4 `getCurrentBottleneck()` — etapa com menor capacidade em production_steps
  - [x] 1.5 `getDailyAlerts()` — gerar lista de alertas automáticos

- [x] 2. Implementar lógica de alertas
  - [x] 2.1 Ruptura: SKU A com stock_coverage < 3
  - [x] 2.2 Produto D em produção: SKU D com stock_finished > 0
  - [x] 2.3 Estoque excessivo: cobertura > 20 dias
  - [x] 2.4 Venda sem margem: net_revenue < variable_cost_total
  - [x] 2.5 Retornar lista de `DailyAlert[]` ordenada por prioridade

- [x] 3. Atualizar página /dashboard com dados reais
  - [x] 3.1 Substituir placeholders "—" pelos valores reais dos KPIs
  - [x] 3.2 Exibir ação crítica do dia (primeiro alerta crítico ou mensagem padrão)
  - [x] 3.3 Lista de alertas abaixo dos KPIs

- [x] 4. Criar componentes do dashboard
  - [x] 4.1 `KpiCard` — card reutilizável com label, valor e variante (default/danger/warning)
  - [x] 4.2 `AlertList` — lista de alertas com ícone de prioridade
  - [x] 4.3 `CriticalAction` — destaque para ação do dia

## Dev Notes

**Tipo DailyAlert já definido em `src/types/index.ts`:**
```typescript
interface DailyAlert {
  type: AlertType
  sku_id?: string
  sku_name?: string
  message: string
  priority: 'critical' | 'warning' | 'info'
}
```

**Regras de alerta (do PRD):**
- ruptura: cobertura < 3 dias → critical
- produto_ruim: SKU D com estoque → warning
- excesso: cobertura > 20 dias → warning
- sem_margem: throughput < 0 → critical
- gargalo: sem produção registrada → warning

**Estrutura de arquivos esperada:**
```
src/app/dashboard/
  page.tsx                    ← atualizar com dados reais
  actions.ts                  ← Server Actions (criar)
src/components/dashboard/
  kpi-card.tsx                ← criar
  alert-list.tsx              ← criar
  critical-action.tsx         ← criar
```

## File List

- `src/app/dashboard/actions.ts` — criado — 5 server actions + helpers privados
- `src/app/dashboard/page.tsx` — modificado — async server component com dados reais
- `src/components/dashboard/kpi-card.tsx` — criado — KpiCard com variantes
- `src/components/dashboard/alert-list.tsx` — criado — AlertList com ícones de prioridade
- `src/components/dashboard/critical-action.tsx` — criado — CriticalAction com fallback

## Dev Agent Record

### Agent Model Used
claude-sonnet-4-6 (@dev / Dex)

### Debug Log References
— sem issues

### Completion Notes
- Todas as 5 server actions implementadas com try/catch para fallback seguro (dashboard nunca quebra por falha de query)
- `buildSkuAgg` e `buildClassificationMap` são helpers privados reutilizados por `getSkusASoldToday` e `getDailyAlerts`
- Classificação requer 30-day window completa para normalização — ambas as funções que precisam de classificação buscam dados independentemente (chamadas em `Promise.all` na página)
- `export const dynamic = 'force-dynamic'` garante sem cache estático conforme AC 5
- `/dashboard` confirmado como `ƒ (Dynamic)` no build de produção
- Lint: 0 erros (3 warnings pré-existentes de TanStack Table e decisions/actions)
- TypeScript: sem erros
- Build: `✓ Compiled successfully`

### Change Log
- Criado `src/app/dashboard/actions.ts` com 5 server actions
- Atualizado `src/app/dashboard/page.tsx` para async com dados reais
- Criado `src/components/dashboard/kpi-card.tsx`
- Criado `src/components/dashboard/alert-list.tsx`
- Criado `src/components/dashboard/critical-action.tsx`
